import { type ItemType } from '@/types/gameState'

export const itemsValue:Record<ItemType, number> = {
  coin: 5,
  star: 6,
  diamond: 7
}

export const ItemsCount:Record<ItemType, number> = {
  coin: 3,
  star: 2,
  diamond: 2
}